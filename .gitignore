# GitHub rejects blobs > 100 MB; keep hires locally or host on Aliyun/OSS
assets/echo-island-hires.glb

# Local deploy credentials (copy from scripts/deploy.env.example)
deploy.env

__pycache__/
*.py[cod]
