# Copy this file to the repo root as deploy.env (same folder as index.html).
# deploy.env is gitignored — do not commit secrets.

DEPLOY_HOST=47.99.57.186
DEPLOY_USER=root
DEPLOY_PATH=/var/www/echo-island/

# Optional
GIT_BRANCH=main
# DEPLOY_PORT=22
# DEPLOY_SSH_KEY=C:\Users\Administrator\.ssh\id_ed25519
